package string

pub fn len(s string) int {
    return 0
}

// 0 if equal, -1 if s1 < s2, 1 if s1 > s2
pub fn compare(s1 string, s2 string) int {
    return 0
}

pub fn concat(s1 string, s2 string) string {
    return ""
}

pub fn is_empty(s string) bool {
    if len(s) == 0 {
        return true
    }
    return false
}

pub fn indexOf(s string, sub string) Option<int> {
    return Option<int>{some: false, value: 0}
}

pub fn contains(s string, sub string) bool {
    return false
}

pub fn startsWith(s string, prefix string) bool {
    return false
}

pub fn endsWith(s string, suffix string) bool {
    return false
}

pub fn charAt(s string, index int) Option<int> {
    return Option<int>{some: false, value: 0}
}

// clamped to the string's bounds
pub fn substr(s string, start int, len int) string {
    return ""
}

// ASCII only
pub fn toUpper(s string) string {
    return ""
}

// ASCII only
pub fn toLower(s string) string {
    return ""
}
