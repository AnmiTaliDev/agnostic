package os

pub fn ArgCount() int {
    return 0
}

// index 0 is the program path
pub fn Arg(index int) string {
    return ""
}

// value is the file descriptor
pub fn OpenRead(path string) Option<int> {
    return Option<int>{some: 0, value: 0}
}

// truncates if the file exists; value is the file descriptor
pub fn OpenCreate(path string) Option<int> {
    return Option<int>{some: 0, value: 0}
}

pub fn Close(fd int) int {
    return 0
}

// value is the number of bytes read
pub fn ReadFd(fd int, buffer int, maxlen int) Option<int> {
    return Option<int>{some: 0, value: 0}
}

// value is the number of bytes written
pub fn WriteFd(fd int, data string) Option<int> {
    return Option<int>{some: 0, value: 0}
}

pub fn Exit(code int) {
    return
}
